#
#
# Default values for internal properties that should not be set in defaults.props.
#

# Session store uses in-memory hash table, instead of relying on a remote server.
# Sessions are stored not as live objects, but as their JSON representations, 
# which emulates the real-life conditions better. 
# No init params, no session expiration
session.store.class.name = com.variant.core.ext.SessionStoreLocalMemory
session.store.class.init = {}

