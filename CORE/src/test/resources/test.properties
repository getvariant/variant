#
# Base properties for JUnit Tests. May be overriden in local-test.properties
#

# Event Persister
persister.class.name = com.variant.ext.persist.H2EventPersister

# jdbc connector
jdbc.url = jdbc:h2:mem:variant;MVCC=true;DB_CLOSE_DELAY=-1;MODE=Oracle
jdbc.user = 
jdbc.password = 