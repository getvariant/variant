#
# Postgres Event flusher for those tests that need to read events back.
#
# DELETE ME?
#event.flusher.class.name = com.variant.core.event.EventFlusherPostgres
#event.flusher.jdbc.vendor = postgres
#event.flusher.jdbc.url = jdbc:postgresql://localhost/variant
#event.flusher.jdbc.user = variant
#event.flusher.jdbc.password = variant
		